# 功能开发需求：运营封面批量生成工具列表页

## 1. 界面与交互概述
- **顶部操作栏**：
  - 【重新上传】按钮
  - 【开始生成】/【批量重试】/【批量导出】按钮
点击重新上传则重新上传excel表格，点击开始生成则开始生成，批量重试则重试生成失败的行，批量导出则将返回的图片打包为压缩包导出到本地
- **主视图**：一个展示解析后 Excel/CSV 数据的表格。
- **状态流转**：
上传前的状态：https://www.figma.com/design/bnn64OzMqpkjjyuKjkd6Wl/AI%E8%BE%85%E5%8A%A9%E7%9A%84%E8%AE%BE%E8%AE%A1%E6%B5%81%E7%A8%8B%E4%BC%98%E5%8C%96?node-id=2973-81&t=Xl0XoBul4YhPyGhT-11
上传完成的状态：https://www.figma.com/design/bnn64OzMqpkjjyuKjkd6Wl/AI%E8%BE%85%E5%8A%A9%E7%9A%84%E8%AE%BE%E8%AE%A1%E6%B5%81%E7%A8%8B%E4%BC%98%E5%8C%96?node-id=2990-91&t=Xl0XoBul4YhPyGhT-11

## 2. 表格列定义与数据源映射说明

请根据以下规则实现表格的每一列数据渲染和逻辑：

| 列名 | 数据从什么地方取？ | 备注/组件行为 |
| :--- | :--- | :--- | :--- |
| **一级标签** | 用户上传的 Excel 文件中的 `一级标签` 字段 | 纯文本展示 |
| **二级标签** | 用户上传的 Excel 文件中的 `二级标签` 字段 | 纯文本展示 |
| **三级标签** |用户上传的 Excel 文件中的 `三级标签` 字段 | 纯文本展示 |
| **标题** |用户上传的 Excel 文件中的 `标题` 字段 | 文本域，支持修改，至多10个字 |
| **老师图** |对应 Excel 的 `老师图`。从excel表格中直接取到图片资源 | 渲染为蓝色可点击的链接，hover可预览原图 |
| **模版** | 根据规则随机到的模版名称，具体随机规则见下方“模版随机规则” |用户点击模版标签时显示所有符合条件的模版，支持点击切换｜
| **生成结果** |**初始/生成中**：从后端接口获取状态（未生成/生成中）。<br>**生成成功**：取接口返回的图片 CDN 链接（如 `poster_url`）。 | **初始**：展示灰色文本“未生成”。<br>**成功**：渲染为正方形的图片预览图，高度固定，保持客制化比例。失败则展示为红色的“生成失败”，并展示失败的错误号和失败详情 |
| **操作** | 由前端触发或根据“生成结果”状态动态改变 | **初始**：展示为短横线 `-`。<br>**生成成功后**：变为蓝色可点击的“重新生成”按钮，点击后单独触发该行的生成接口。 |

## 3. 模版随机规则
根据用户上传的 Excel 数据中是否包含“老师图片”，自动执行以下推荐与校验策略：
I.自动分流推荐策略
* **策略 A（包含老师图片）**：如果表格某行中**有**老师图片，系统会自动随机匹配一个**“带真人形象”**的模版。
* **策略 B（未含老师图片）**：如果表格某行中**没有**老师图片，系统会自动随机匹配一个**“不带真人形象”**的模版。

II. 2. 异常阻断提示（边界情况）
* **触发条件**：当表格某行**没有**老师图片，且该课程分类下**只有**带真人形象的模版时。
* **系统行为**：阻断生成，并在对应行弹出提示：`“该分类下仅支持真人模版，请额外上传一张老师图片。”

III.3. 人工干预机制（二次确认）
* **模版检查**：批量推荐完成后，用户可以对系统随机选出的模版进行预览检查。
* **手动换掉**：如果用户不喜欢系统推荐的风格，支持点击该行的“模版”列，**点击下拉切换**为其他可用模版。


## 4. 核心业务逻辑与技术要求（请 AI 严格遵守）

1. **数据驱动**：前端组件需要设计一个 `tableData` 状态（State），每行包含 `id`, `level1`, `level2`, `level3`, `title`, `teacherImg`, `templateName`, `status`, `resultImg` 字段。
2. **状态联动**：
   - 当点击顶部的【开始生成】时，批量触发后端 AI 生成接口，并将每行的 `status` 改为 `generating`（前端可以加一个 Loading 效果）。
   - 接口返回成功后，更新对应行的 `status` 为 `success`，并把图片链接赋给 `resultImg`，此时该行自动切换为右图样式。
